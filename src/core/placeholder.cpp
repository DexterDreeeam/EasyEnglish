// Placeholder translation unit so the core static library builds before
// any real modules are added. The first real module (iter-001-storage)
// will remove this file.
namespace easyenglish::core {
int placeholder() {
    return 0;
}
}  // namespace easyenglish::core
