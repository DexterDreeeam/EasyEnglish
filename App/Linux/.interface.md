⬆️ [App](../.interface.md)

# Linux Module — Interface

> **Phase 1 status:** placeholder. The contract below will be filled when the
> implementation iteration starts. Reserved here so cross-references from the
> root `.interface.md` resolve.

## Public

(none — Phase 1 placeholder)

Planned surface (subject to change before the implementation ADR is written):

`rust
pub fn run(config: ee_core::Config) -> std::io::Result<()>;
`

This will be the single entry point a future bin crate invokes after parsing
`product.json`. Internally it owns the tray icon, the global-hotkey handler,
the winit event loop, and the egui frame loop.

## Private

(none — Phase 1 placeholder)
