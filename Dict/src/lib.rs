//! `ee-dict` — offline English→Chinese dictionary bundle and access layer.
//!
//! Phase 1 currently exposes nothing; iter-013 will add `Entry`, `DictError`,
//! and `DictStore`. See `Dict/.design.md` for the design and
//! `Dict/.interface.md` for the API contract.

#![forbid(unsafe_code)]
#![warn(missing_docs)]

// Intentionally empty — populated in iter-013.
